      {% if thumb or images %}  
      <div class="slider-img-nav col-md-2 hidden-sm hidden-xs">
        <ul class="slider-nav">
          {% if thumb %}
          <li><img src="{{ additional }}" title="{{ heading_title }}" alt="{{ heading_title }}" /></li>
          {% endif %}
          {% if images %}
          {% for image in images %}
          <li class="image-additional"><img src="{{ image.additional }}" title="{{ heading_title }}" alt="{{ heading_title }}" /></li>
          {% endfor %}
          {% endif %}
        </ul>	
      </div>

      <div class="slider-img col-md-10 hidden-sm hidden-xs">
        <ul class="thumbnails slider-for">
          {% if thumb %}
          <li>
          <div class="easyzoom easyzoom--overlay"><a class="thumbnail" href="{{ popup }}" title="{{ heading_title }}"><img src="{{ thumb }}" title="{{ heading_title }}" alt="{{ heading_title }}" /></a></div>
          </li>
          {% endif %}
          {% if images %}
          {% for image in images %}
          <li>
          <div class="easyzoom easyzoom--overlay"><a class="thumbnail" href="{{ image.popup }}"  data-standard="{{ image.popup }}" data-resize = "{{ image.resize }}" title="{{ heading_title }}"> <img src="{{ image.thumb }}" title="{{ heading_title }}" alt="{{ heading_title }}" /></a></div>
          </li>
          {% endfor %}
          {% endif %}
        </ul>	
      </div>
      {% endif %}
            <div class="col-xs-12 col-sm-6 hidden-lg hidden-md">
        {% if thumb or images %}
        <ul class="thumbnails single-slide">
          {% if thumb %}
          <li>
            <a class="thumbnail" href="{{ popup }}" title="{{ heading_title }}"><img src="{{ thumb }}" title="{{ heading_title }}" alt="{{ heading_title }}" /></a>
          </li>
          {% endif %}
          {% if images %}
          {% for image in images %}
          <li>
            <a class="thumbnail" href="{{ image.popup }}" data-resize = "{{ image.resize }}" title="{{ heading_title }}"> <img src="{{ image.thumb }}" title="{{ heading_title }}" alt="{{ heading_title }}" /></a>
          </li>
          {% endfor %}
          {% endif %}
        </ul>	
        {% endif %}
      </div>

 